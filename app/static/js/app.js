requirejs.config({
  // "baseUrl": "js/lib",
  "paths": {
    // "app": "../app",
    "fs": "https://cdnjs.cloudflare.com/ajax/libs/mocha/1.0.2/package/lib/browser/fs.js"
  }
});

// Load the main app module to start the app
// requirejs(["app/main"]);